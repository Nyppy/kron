import React from 'react';
import RecoveryPassContent from './recovery-pass';
import {Header, Footer} from '../../components/common';

export default function RecoveryPass () {
    return (
        <div>
            <Header />
            <RecoveryPassContent />
            <Footer />
        </div>
    )
}